<nav class="navbar navbar-expand-lg">
	<div class="container-fluid">
		<a class="navbar-brand text-white" href="#">WS</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarNav">
			<ul class="navbar-nav">
				<li class="nav-item">
					<a class="nav-link active text-white" aria-current="page" href="{{ path('app_index') }}">Wild Séries</a>
				</li>

				<div class="dropdown">
					<a class="btn text-white dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
						Catégories
					</a>
					<ul class="dropdown-menu">
						{{ render(controller('App\\Controller\\DefaultController::navbarTop')) }}</ul>

				</div>
				<li class="nav-item">
					<a class="nav-link text-white" href="#">Acteurs</a>
				</li>

				{% if app.user %}
					{% trans with {'%username%': app.user.email} %}
					hello_username
					{% endtrans %}
					!
					<li class="nav-item">
						<a class="nav-link text-white" href="{{ path('app_logout') }}">
							{{ 'account.logout'|trans }}</a>
					</li>
					<li class="nav-item">
						<a class="nav-link text-white" href="{{ path('app_logout') }}">
							Mon compte</a>
					</li>
				{% else %}
					<li class="nav-item">
						<a class="nav-link text-white" href="{{ path('app_login') }}">
							{{ 'account.login'|trans }}</a>
					</li>
					<li class="nav-item">
						<a class="nav-link text-white" href="{{ path('app_register') }}">
							Créer un compte</a>
					</li>
				{% endif %}
				<div class="dropdown">
					<a class="btn text-white dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
						Langues
					</a>
					<ul class="dropdown-menu">
						<li>
							<a class="dropdown-item" href="{{ path('app_index', {_locale: 'fr'}) }}">Français</a>
						</li>
						<li>
							<a class="dropdown-item" href="{{ path('app_index', {_locale: 'en'}) }}">Anglais</a>
						</li>
					</ul>
				</div>
			</ul>
		</div>
	</div>
</nav>
